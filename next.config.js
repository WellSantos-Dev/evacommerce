/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'www.eva-test-api.plataformaeva.com'],
  },
}

module.exports = nextConfig
